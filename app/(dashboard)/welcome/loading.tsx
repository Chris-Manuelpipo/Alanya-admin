// Server Component — squelette de chargement dédié à la page Bienvenue (RSC).
import { WelcomePageSkeleton } from "@/components/skeletons/welcome-skeleton";

export default function WelcomeLoading() {
  return <WelcomePageSkeleton />;
}
