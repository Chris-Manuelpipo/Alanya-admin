// Server Component — squelette de chargement dédié aux analytics (RSC).
import { AnalyticsPageSkeleton } from "@/components/skeletons/analytics-skeleton";

export default function AnalyticsLoading() {
  return <AnalyticsPageSkeleton />;
}
