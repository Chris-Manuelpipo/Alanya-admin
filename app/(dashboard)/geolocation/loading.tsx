// Server Component — squelette de chargement dédié à la géolocalisation (RSC).
import { GeolocationPageSkeleton } from "@/components/skeletons/geolocation-skeleton";

export default function GeolocationLoading() {
  return <GeolocationPageSkeleton />;
}
